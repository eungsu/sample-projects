<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.UserMapper">
	<select id="getUserById" parameterType="string" resultType="com.example.vo.User">
		select
			user_id as id,
			user_encrypt_password as encryptPassword,
			user_name as name,
			user_email as email,
			user_created_date as createdDate,
			user_updated_date as updatedDate
		from
			sample_users
		where
			user_id = #{value}
	</select>
	
	<select id="getUserRolesByUserId" parameterType="string" resultType="com.example.vo.Role">
		select
			user_id as id,
			user_role_name as name
		from
			sample_user_roles
		where
			user_id = #{value}
	</select>
	
	<insert id="insertUser" parameterType="com.example.vo.User">
		insert into sample_users
		(user_id, user_encrypt_password, user_name, user_email, user_created_date, user_updated_date)
		values
		(#{id}, #{encryptPassword}, #{name}, #{email}, now(), now())
	</insert>
	
	<insert id="insertUserRole" parameterType="com.example.vo.Role">
		insert into sample_user_roles
		(user_id, user_role_name)
		values 
		(#{id}, #{name})
	</insert>
</mapper>